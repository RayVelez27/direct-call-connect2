

## Make SearchOverlay Style Updates

Three small styling changes to the `SearchOverlay` component:

1. **Background color**: Change the overlay background from teal (`#17EAD9`) to blue (`#6078EA`) when focused
2. **Title text color**: Change both title texts from blue (`#6078EA`) to white
3. **Remove "Searching..." text**: Remove the second heading that says "Searching..."

All changes are in `src/components/SearchOverlay.tsx`.

